# Zwift Race Finder — direnv environment
# IDs are not secrets; tokens stay in strava_config.json (gitignored)

# Zwift / ZwiftPower
export ZWIFTPOWER_PROFILE_ID=1106548

# Strava (non-secret identifiers only)
export STRAVA_ATHLETE_ID=2996494
export STRAVA_CLIENT_ID=161572
